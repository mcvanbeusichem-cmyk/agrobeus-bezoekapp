# Database (SQLite voor lokale start, vervang later door PostgreSQL URL)
DATABASE_URL="file:./dev.db"

# Mail (later invullen bij koppeling met mailservice)
# RESEND_API_KEY="re_..."
# MAIL_FROM="marco@agrobeusconsulting.nl"
